{
  "name": "color-wheel-demo",
  "main": "index.js",
  "scripts": {
    "build": "NODE_ENV=production ../node_modules/.bin/rollup -c",
    "serve": "docker run --name cw-nginx -p 8000:80 -v $PWD:/usr/share/nginx/html:ro nginx:alpine || docker start -ia cw-nginx"
  },
  "dependencies": {
    "react-dom": "^16.2.0"
  },
  "devDependencies": {
    "rollup-plugin-commonjs": "^9.1.0",
    "rollup-plugin-node-resolve": "^3.3.0",
    "rollup-plugin-replace": "^2.0.0",
    "rollup-plugin-uglify": "^3.0.0"
  }
}
